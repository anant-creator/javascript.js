for (let exercise = 1; exercise < 6; exercise++) {
    console.log(`----- Exercise Set No. ${exercise} -----`);

    for (let lift = 1; lift <= 5; lift++) {
        console.log(`Exercise ${exercise} -- Dumble Lift no. ${lift} --`);
    }
}
